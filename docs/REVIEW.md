# REVIEW — push 前に人間が見る1枚

**これは人間が週1レビューで読む唯一の面。** ループ（`/auto-dev`）が毎サイクル**上書き集約する**（追記でなく最新化。履歴・逐語は git / JOURNAL が持つ）。

> 🧹 **節構成はこの4節に固定**（`tests/review-shape.test.ts` が機械で守る）。①②③は「人間しか答えられないこと」だけ。**④は報告＝読み飛ばしてよい**。
> ③は**3件が上限**（増やすなら古いものを落とす＝毎サイクル選び直す）。
> バイト予算は `tests/docs-budget.test.ts`。

---

## ① 判断（あなたが動くのはここだけ）

**この main の commit 群を upstream に push（＝GitHub Pages へデプロイ）してよいか。人間の判断はこれだけ。**

- **中身**: `origin/main` より **6 commits 先**。前回から増えたのは1つ——**いただいた「X の共有カード」を作りました**（リンクを貼ると、主人公6人の似顔絵が並んだ横長の絵とタイトルが出ます）。もう1つは第7作『渋沢栄一』の第五章（前回ぶん・画面には出ません）。
- **画面に出るか**: 遊ぶ画面は**変わりません**。変わるのは2つ——**共有したときに出る絵**と、**ブラウザのタブ／ブックマークの題名**（第1作のまま「ひでなが 〜天下人の弟…〜」だったのを「歴史の 人に、なりきる。｜ rekishi-play」に直しました）。
- **機械検査**: `npm run check` 0 errors ／ `npm run verify` **1083 tests 全 pass**。
- **セーブ**: **形は変えていません**。家族の記録はそのまま引き継がれます。
- **押し方**: `git push`（fast-forward。force 不要）。**共有カードは push して公開されるまで X 側から見えません**（X はネット上の画像を取りに来るため）。

## ② 止めるなら今のうち（**沈黙＝GO**。止めたいときだけ観察メモへ一行）

- **共有カードは「作品集ぜんぶで1枚」で作りました**（③1 の絵を見てください） — 作品ごとに違う絵にする案もありましたが、いまは1ページのアプリで URL が作品ごとに分かれていないため、**1枚でないと嘘になります**（どの作品のリンクを貼っても同じ URL）。作品ごとの絵が要るなら、先に「作品ごとの URL」を作る話になります＝**やるなら一行ください**（無記入＝1枚のまま）。
- **『渋沢栄一』は残り6章をこの調子で書きます** — 芯は「**有名な決裂の場面は、たぶん後から作られた話**」＝子どもが直前に自分で体験した場面を、次の場面で疑わせる作り。**別の人物がよい／既存6作の手直しを優先**なら一行ください。
- **⛔ 『政子』に清盛を出す枠がありません（一行いただければ動きます・急ぎません）** — 『家康』には秀長を出しました。同じことを**『政子』に清盛**でやりたい（清盛は頼朝を助命した人＝政子の物語の起点）のですが、『政子』は**人物カードが16人ちょうど＝「人は絞る」の上限**です。上限を触るのは AI の禁じ手なので止めてあります。**(i) 政子だけ17人にしてよい／(ii) 上限を17〜18人に上げてよい／(iii) 出さなくてよい**（無記入＝(iii) で進みます）。
- **⛔ CRITERIA 改訂案（AI は本文を触れません・放置しても動作に影響なし）** — (d) `docs/CRITERIA.md:13` の G3「既存セーブ互換を壊さない（`hidenaga_save_v1`）」は作品横断アカウント化で**対象が消えました**→「セーブスキーマを壊す変更は人間の明示指示があるときだけ（キー: `rekishi_play_account_v1`）」への差し替え。(a) G10「主線本文が説明に堕ちていない＝体験予算が通る」の新設。(c) 評価ペルソナは読了前提にせず離脱段落を報告。
- **⛔ VISION 1行**（適用はあなた・やらなくても動作に影響なし）— `docs/VISION.md:39`「マージ・デプロイの承認」→**「upstream への push（＝デプロイ）の承認——自律開発ループにおける唯一の人間判断ゲート」**。feature ブランチ廃止で「マージ」が実態として存在しないため。

## ③ 実機で見てほしいこと＝上位3件（これ以上は増やしません）

**書き戻し先＝`docs/BACKLOG.md` 末尾の「## 観察メモ」節に箇条書きで一行**（文章は雑でよく、古い行は消して置き換えてよい）。

1. **★共有カードの絵そのもの**（`public/og.png` を開いてください。1200×630） — 見てほしいのは **(a) この1枚で「何のサイトか」が伝わるか (b) 見出し「歴史の 人に、なりきる。」でよいか (c) 6人の顔と名前の並びに違和感がないか**。文言も絵も**一行いただければ作り直します**（絵は自動生成なので作り替えは軽いです）。push 後に X で実際に貼って確認するのが本番です。
2. **名前が「自分だ」とわかりますか**（前々回の対応・まだ観察をいただいていません） — トップバー右上が **`🐢 ゆい ⚙` のピル**になり、章の中でも常に名前が出ます。押すと `⇄ ほかの 子に かわる`。見てほしいのは **(a) 子どもが自分の名前に気づくか (b) 切替をそこで見つけられるか (c) 動物のしるしは要るか**。
3. **どこで手が止まったか**（章名だけのメモで十分＝いちばん欲しい観察データ）。とくに**家康 章一・章六前半**、そして**遊ぶ順番**（どの人から始めたか・途中で別の人に移ったか）。

## ④ 以下は報告（読み飛ばし可）

### 共有カードの作り（今回）

- 絵は**作品一覧から自動で組み立てています**——主人公の似顔絵・名前・生没年は作品データそのままなので、**第7作を出したら焼き直すだけ**で7人になります（焼き忘れるとテストが赤くなって手順を教えます）。
- 絵の中身: 和紙の地に見出し「歴史の 人に、なりきる。」／「ひとりの 一生を 7章で 生きる、小5〜中1むけの ブラウザ教材」／6人の顔・名前・生没年／下段に「えらぶのは きみ。そのあとで「史実では…」が ひらく。」と配信 URL。
- **子どもの名前や進みぐあいが外に出ることはありません**（絵は固定の1枚で、端末の記録は一切使いません）。
- 技術上の注意を1つだけ: この絵の住所（絶対 URL）は **https://hidai.github.io/rekishi-play/ 前提で焼いています**。独自ドメインへ移すときは焼き直しが要ります（`SITE_URL=... npm run build`）。

### 前回書いた章の中身（渋沢 第五章「舟の うえ」・再掲の要点だけ）

- 隅田川の舟の上で岩崎弥太郎が「二人で手を組めば日本の商いは思いのままだ」と誘う。受ければ日本一の金持ち、断れば海の王者と正面から争う。史実の渋沢は断り、みんなで株を出し合う船会社で競い、両社とも傷つき、岩崎の急死した年に**2社は合併**（今の日本郵船）。
- **岩崎を悪役にしていません**（「力を一つに集めた者しか海の道は守れない」＝もう一つの正解）。三菱の後ろだて（国の汽船）を暴くのと同時に、渋沢側の後ろだて（政府・三井）も書いています。
- 小5ペルソナは『秀長』の方を読みたいと答えました（中1は渋沢）。理由は「株・合本・保険は目に見えないので絵が浮かばない」＝残り6章は**制度のことばを1つ出したら具体物を1つ**出す規則で書きます。

### 各作品の芯（章立ての詳細は git / docs/design）

- **家康 全7章**＝各章が「よく知られた家康像」を1枚ずつ剥がす（三方ヶ原＝唯一「正解を選ぶと負ける」岐路）。
- **ダビンチ 全7章**＝謎は「なぜ天才はほとんど"完成"させなかったのか」。**観察ビュー**（絵をなぞる）・**つながり図鑑**。
- **政子 全7章**＝地図がほとんど使えず主装置は**「人の図」「席の図」「対面の場」**。むすびは「人は代わっても席は残った」。
- **渋沢 1/7章**＝史料批判の向きは「**本人が語りすぎた／聖人化された人を、生身に戻す**」。

### 余力があれば見る観点（③を消化してから・順不同）

家康＝終章の死の温度／脱糞の逸話の可否（嫌なら外します）。**☆マークを「よくできました」と読み違えないか**。確度の ◎ / △ のしるしの意味が子どもに伝わっているか（説明を置く場所がまだありません）。左上のタイトルがひらがななのは意図的です（`katsu` の「かつ」だけ据わりが悪い＝「かいしゅう」等に変えてよければ一行）。
